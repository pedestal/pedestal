(ns io.pedestal.interceptor.provider)

(defprotocol InterceptorProvider

  ;(provider-context [t ])
  ;(async-supported? [t])
  ;(go-async [t])
  ;(round-trip-chain [t interceptors])
  ;(one-way-chain [t interceptors])

  )
