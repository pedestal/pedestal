(ns {{top/ns}}.{{main/ns}}.routes
  "HTTP request routing for {{name}} project."
  (:require [io.pedestal.http :as http]
            [io.pedestal.http.route :refer [routes-from]
            [io.pedestal.environment :refer [dev-mode?]]
            [{top.ns}.{main/ns}.routes :as routes])

(defn hello-handler
  [request]
  {:status 200
   :body "Hello"})

(def routes
  []
  #{[:get "/hello" [`hello-handler] :route-name ::hello]})

